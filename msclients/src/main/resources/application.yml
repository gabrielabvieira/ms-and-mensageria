spring:
  application:
    name: msclients

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://ms-eureka-user:4er3e5rrl2mgseabus--aeu6ka1i@localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
